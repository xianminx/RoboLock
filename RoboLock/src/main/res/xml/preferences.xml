<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <PreferenceCategory
        android:key="pref_key_storage_settings"
        android:title="RoboLock">
        <CheckBoxPreference
            android:defaultValue="false"
            android:key="pref_robolock_autolock"
            android:summary="Requires authentication when launching the app."
            android:title="Auto lock" />
        <EditTextPreference
            android:defaultValue="3"
            android:dependency="pref_robolock_enabled"
            android:dialogTitle="Set Auto Lock Time"
            android:key="pref_robolock_auto_lock_time"
            android:summary="0 means lock immediately, and -1 means no automatic lock. A positive integer means lock the screen only when the app is switch to the background and wait for that minutes"
            android:title="Auto lock time" />
    </PreferenceCategory>
</PreferenceScreen>